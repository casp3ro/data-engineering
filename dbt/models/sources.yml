version: 2

sources:
  - name: silver
    description: "Spark-cleaned silver Delta tables stored in MinIO"
    tables:
      - name: listings
        description: "Cleaned, deduplicated vehicle listings produced by the Spark silver job"

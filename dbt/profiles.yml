lingua:
  target: dev
  outputs:
    dev:
      type: duckdb
      # sciezka do tej samej hurtowni, ktora napelnia load_raw.py
      path: ../warehouse/lingua.duckdb
      schema: main
      threads: 4
